import 'package:flutter/material.dart';

class GenerateRoutePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("GenerateRoutePage"),
      ),
      body: Center(
        child: Text("GenerateRoutePage"),
      ),
    );
  }
}
